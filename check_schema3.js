const { createClient } = require('@supabase/supabase-js');
const dotenv = require('dotenv');

dotenv.config({ path: '.env.local' });

const supabaseUrl = process.env.VITE_SUPABASE_URL || '';
const supabaseKey = process.env.VITE_SUPABASE_ANON_KEY || ''; // we need service_role for creating tables? No, REST API does not allow CREATE TABLE at all.

console.log("REST API cannot execute CREATE TABLE statements.");
